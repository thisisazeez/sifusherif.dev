django>=4.2,<5.0
python-decouple>=3.8      # reads .env file
whitenoise>=6.6           # static file serving
django-redis>=5.4         # Redis cache backend (production)
redis>=5.0                # Redis client
markdown>=3.5             # Markdown → HTML renderer
pymdown-extensions>=10.0  # fenced_code, superfences extras
pygments>=2.17            # syntax highlighting in code blocks
gunicorn>=21.0            # WSGI server (production)
pillow>=10.0              # image handling (future-proofing)
